const { default: colors } = require("./colors");

module.exports ={
    head1: {
        fontSize: 30,
        color: colors.secondary,
        textAlign: "center",
        fontWeight:600
    },
    head2: {
        fontSize: 20,
        color: colors.secondary,
        textAlign: "left",
        fontWeight:600,
        
    }
}